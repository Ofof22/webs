                <!-- views/panel.ejs -->

                <!DOCTYPE html>
                <html lang="en">
                <head>
                    <meta charset="UTF-8">
                    <meta http-equiv="X-UA-Compatible" content="IE=edge">
                    <meta name="viewport" content="width=device-width, initial-scale=1.0">
                    <style>
                        body {
                            font-family: Arial, sans-serif;
                            margin: 20px;
                        }

                        h1 {
                            color: #333;
                        }

                        h2 {
                            color: #555;
                        }

                        div {
                            margin-bottom: 30px;
                        }

                        ul {
                            list-style-type: none;
                            padding: 0;
                        }

                        li {
                            display: flex;
                            justify-content: space-between;
                            align-items: center;
                            border-bottom: 1px solid #ddd;
                            padding: 10px;
                            margin-bottom: 5px;
                        }

                        form {
                            display: inline-block;
                        }

                        button {
                            background-color: #e74c3c;
                            color: #fff;
                            border: none;
                            padding: 8px 12px;
                            cursor: pointer;
                            font-size: 14px;
                        }

                        button:hover {
                            background-color: #c0392b;
                        }
                    </style>
                    <title>Panel</title>
                </head>
                <body>
                    <h1>Panel</h1>
                    <div>
                        <h2>Üyeler</h2>
                        <ul>
                            <% for (const user of users) { %>
                                <li>
                                    <%= user.kullaniciAdi %> - <%= user.sifre %> - İzin Seviyesi: <%= user.perm %>
                                    <form action="/manageUser" method="POST">
                                        <input type="hidden" name="kullaniciAdi" value="<%= user.kullaniciAdi %>">

                                        <!-- Güncelleme işlemi -->
                                        <label for="permission">Yeni İzin Seviyesi (0-3): </label>
                                        <input type="number" name="permission" min="0" max="3">

                                        <!-- Silme işlemi -->
                                        <button type="submit" name="action" value="delete">Sil</button>

                                        <!-- Güncelleme işlemi için buton -->
                                        <button type="submit" name="action" value="update">Güncelle</button>
                                    </form>
                                </li>
                            <% } %>
                        </ul>
                    </div>

                    <div>
                        <h2>Kitaplar</h2>
                        <ul>
                            <% for (const kitap of kitaplar) { %>
                                <li>
                                    <%= kitap.kitapAdi %>
                                    <form action="/deleteKitap" method="POST">
                                        <!-- Kitap adını "kitapAdi" olarak gönder -->
                                        <input type="hidden" name="kitapAdi" value="<%= kitap.kitapAdi %>">
                                        <button type="submit">🗑️</button>
                                    </form>
                                </li>
                            <% } %>
                        </ul>
                    </div>
                </body>
                </html>
